## DTURL, a local url shortener  

### Project Premise 

This crate is a Rust-based url shortener, which generates short url and stores url mappings locally using SQLite. The interface generates and stores urls via in Iced window. 


## Upcoming features
- Adding a GET request to test url path before generating/storing short url in database
- Redis caching
